version: 2

sphinx:
  configuration: ForDocumentation/docs/conf.py

python:
  install:
    - method: pip
      path: .

# Optional: if you need extra dependencies
#  requirements_file: requirements.txt
